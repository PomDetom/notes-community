server:
  port: 8010
spring:
  application:
    name: notes-email
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: ${nacos.address:127.0.0.1}:8848
        file-extension: yml
        group: SERVICE-GROUP